<body class="test">
  <%= render "partials/navbar"%>

    <h1><%= @city.name %> internet speed</h1>
<%= @city.internet %>
<%= link_to "city guide", city_guide_path(@city) %>

<%= link_to 'Back', cities_path %>
  <div class="grid">
    <div class="sidebar">sidebar</div>
    <div class="head">no</div>
    <div class="proscons">si</div>
    <div class="visa">senior</div>
    <div class="gettingto">come</div>
</div>
<body>
